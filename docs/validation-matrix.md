# Validation Matrix

Date: 2026-06-15

This matrix lists the current validation surfaces for Astro Suite. The suite treats local
deterministic workflows and unit-level backend contracts as required regression references.
Optional runtime commands are smoke-gated adapter checks and are not required local release gates
unless the matching runtime is intentionally installed for a live validation campaign.

## Required Local Gates

These gates must run without optional Orekit, TudatPy, RocketPy, Dymos/OpenMDAO, or JAX runtimes.
Rows that exercise optional engines through live CLI commands belong in `Optional Backend Gates`.

| Area | Command | Expected Result |
| --- | --- | --- |
| Test suite | `python -m pytest -q` | All non-live tests pass; Orekit live tests remain skipped unless explicitly enabled. |
| Lint | `python -m ruff check .` | No lint findings. |
| Types | `python -m mypy` | Strict type checking passes. |
| Scenario validation | `astro validate examples/scenarios/leo_two_body.yaml` | Valid scenario message for `leo-two-body`. |
| Local propagation | `astro propagate examples/scenarios/leo_two_body.yaml --backend local --output /tmp/astro-local-trajectory.json` and `astro propagate examples/scenarios/leo_eccentric_two_body.yaml --backend local --output /tmp/astro-eccentric-trajectory.json` | Writes `Trajectory` products with `backend = "local"` and root-located apsis event metadata, including an interior apoapsis root in the eccentric LEO case. |
| Tudat native variational covariance contract | `python -m pytest tests/astro_backends/test_tudat_propagation.py::test_propagate_tudat_uses_default_native_variational_runner_when_requested tests/astro_backends/test_tudat_propagation.py::test_propagate_tudat_uses_native_variational_runner_when_requested tests/astro_backends/test_tudat_propagation.py::test_propagate_tudat_requires_native_variational_runner_when_requested -q` | Validates the `covariance_state_transition_model: tudat_variational` contract: Tudat refuses to silently downgrade to finite differences when the optional variational API is unavailable, builds the default TudatPy initial-state parameter set and variational-equation solver when available, and maps native state-transition/covariance samples into the suite covariance-history product with native variational provenance. |
| Local MEO/GEO propagation | `astro propagate examples/scenarios/meo_two_body.yaml --backend local --output /tmp/astro-meo.json` and `astro propagate examples/scenarios/geo_two_body.yaml --backend local --output /tmp/astro-geo.json` | Writes medium-Earth and geosynchronous-radius two-body trajectories. |
| Local finite burn | `astro propagate examples/scenarios/leo_finite_burn.yaml --backend local --output /tmp/astro-finite-burn.json` and `astro propagate examples/scenarios/leo_velocity_aligned_burn.yaml --backend local --output /tmp/astro-velocity-aligned-burn.json` | Writes maneuver start/end events, inertial and velocity-aligned thrust-vector mass-flow provenance metadata, sample mass history, and commanded-attitude quaternion samples for maneuvered trajectories. |
| Rigid-body attitude and closed-loop ACS propagation | `astro propagate-attitude examples/attitude/rigid_body_torque.yaml --output /tmp/astro-attitude-dynamics.json`, `astro propagate-attitude examples/attitude/closed_loop_pd.yaml --output /tmp/astro-attitude-control.json`, and `astro propagate-attitude examples/attitude/closed_loop_sensor_actuator.yaml --output /tmp/astro-attitude-sensor-actuator.json` | Writes suite attitude dynamics products with quaternion, body-rate, applied-torque samples, diagonal inertia provenance, and either scheduled torque, bounded quaternion-error PD control metadata, or deterministic sensor-bias plus actuator scale/bias/deadband screening metadata. Closed-loop products include final/max attitude and rate error, configured tolerances, control status, torque saturation counts, and actuator deadband counts. This validates deterministic ACS product wiring without claiming a flight-qualified spacecraft actuator/sensor simulation. |
| Local covariance | `astro propagate examples/scenarios/leo_covariance.yaml --backend local --output /tmp/astro-covariance.json`, `astro propagate examples/scenarios/leo_variational_covariance.yaml --backend local --output /tmp/astro-variational-covariance.json`, and `astro propagate examples/scenarios/leo_j2_variational_covariance.yaml --backend local --output /tmp/astro-j2-variational-covariance.json` | Writes deterministic covariance history screening samples with finite-difference, opt-in two-body variational, or opt-in J2 variational state-transition matrices, accumulated state-transition matrices, symmetric covariance/process-noise matrices, and optional white-acceleration process-noise matrices. This validates suite covariance product wiring and first-order screening behavior; it is not drag/SRP/third-body production covariance validation. |
| Ephemeris interchange | `astro export-trajectory /tmp/astro-local-trajectory.json --format csv --output /tmp/astro-local-trajectory.csv`, `astro export-trajectory /tmp/astro-local-trajectory.json --format oem --output /tmp/astro-local-trajectory.oem`, `astro export-trajectory /tmp/astro-local-trajectory.json --format opm --output /tmp/astro-local-state.opm`, `astro export-trajectory /tmp/astro-velocity-aligned-burn.json --format aem --output /tmp/astro-attitude.aem`, `astro import-trajectory /tmp/astro-local-trajectory.oem --format oem --scenario examples/scenarios/leo_two_body.yaml --output /tmp/astro-local-trajectory-from-oem.json`, `astro import-trajectory examples/trajectories/leo_initial_state.opm --format opm --scenario examples/scenarios/leo_two_body.yaml --output /tmp/astro-local-state-from-opm.json`, and `astro import-trajectory /tmp/astro-attitude.aem --format aem --scenario examples/scenarios/leo_velocity_aligned_burn.yaml --state-trajectory /tmp/astro-velocity-aligned-burn.json --output /tmp/astro-attitude-from-aem.json` | Writes CSV, CCSDS OEM KVN position/velocity sample histories, CCSDS OPM KVN single-state orbit messages with optional covariance blocks, and CCSDS AEM KVN quaternion attitude samples when trajectory attitude is present, then imports OEM/OPM state products with scenario force-model context and AEM attitude-only products onto a state-bearing suite trajectory. |
| Conjunction screening | `astro screen-conjunction /tmp/astro-local-trajectory.json /tmp/astro-secondary-trajectory.json --threshold-km 1.0 --hard-body-radius-km 0.02 --probability-method integrated --output /tmp/astro-conjunction-screening.json` and `astro assess-conjunction /tmp/astro-conjunction-screening.json --output /tmp/astro-conjunction-assessment.json` | Writes a sample-aligned closest-approach screening product with TCA epoch, miss distance, relative speed, threshold status, relative-state metadata, and an encounter-plane Gaussian disk integral when both trajectories carry TCA covariance; then writes a conservative readiness report that classifies the result as screening-only, requiring review, or an operational candidate without claiming external production validation. |
| Earth-orientation ingest | `astro import-earth-orientation examples/eop/finals2000A_excerpt.txt --format iers-finals --source finals2000A-example --output /tmp/astro-earth-orientation.json` | Converts IERS finals/finals2000A-style rows into suite `EarthOrientationConfig` JSON with UT1-UTC and polar-motion samples. |
| Local OD ingest | `astro estimate-measurements examples/scenarios/leo_two_station_od.yaml examples/measurements/leo_two_station_od_measurements.json --output /tmp/astro-local-estimate.json` | Writes `EstimateResult` from explicit measurements. |
| Radiometric measurement synthesis | `astro synth-measurements examples/scenarios/leo_doppler.yaml --backend local --output /tmp/astro-doppler-measurements.json`, `astro export-measurements /tmp/astro-doppler-measurements.json --format tdm --output /tmp/astro-doppler-measurements.tdm`, `astro synth-measurements examples/scenarios/leo_radiometric_links.yaml --backend local --output /tmp/astro-radiometric-links.json`, `astro synth-measurements examples/scenarios/leo_radiometric_media.yaml --backend local --output /tmp/astro-radiometric-media.json`, and `astro synth-measurements examples/scenarios/leo_radiometric_weather_frequency.yaml --backend local --output /tmp/astro-radiometric-weather-frequency.json` | Writes mixed range/one-way Doppler records and iterative linearized two-way/three-way range/range-rate records with explicit path, vacuum light-time, transmit/reflection/receive offset, iteration-count, and constant or weather/frequency media-correction metadata; explicit multi-leg and Hz Doppler records round-trip through TDM with suite metadata extensions. |
| DSN tracking ingest | `astro import-dsn-tracking examples/measurements/dsn_tracking_normalized.csv --output /tmp/astro-dsn-tracking-measurements.json` and `astro import-dsn-kvn-tracking examples/measurements/dsn_tracking_kvn.txt --output /tmp/astro-dsn-kvn-tracking-measurements.json` | Imports normalized CSV and strict KVN-style ODF/TNF DSN tracking rows into suite measurement JSON with station, spacecraft, observable, sigma, participant-path, transmitter, source-format, tracking-format, suite-owned bridge-scope, and native-decoder provenance. These are suite-owned DSN bridge formats, not native binary ODF/TNF decoders. |
| DSN binary bridge ingest | `python -m pytest tests/astro_od/test_dsn_tracking.py::test_load_dsn_binary_tracking_measurements_maps_fixed_records tests/astro_od/test_dsn_tracking.py::test_load_dsn_binary_tracking_measurements_rejects_bad_magic tests/astro_od/test_dsn_tracking.py::test_load_dsn_binary_tracking_measurements_rejects_unsupported_codes tests/astro_cli/test_cli.py::test_import_dsn_binary_tracking_command_writes_measurement_json -q` | Validates the suite-owned `ASTRODSN1` fixed-record binary DSN tracking bridge and CLI ingest path for ODF/TNF-labeled records with typed observable and units codes, explicit suite-owned bridge provenance, and actionable malformed-bridge rejection for bad magic and unsupported tracking/observable/unit codes. This is not a standards-grade NASA ODF/TNF binary decoder. |
| DSN calibration summary | `astro dsn-calibration examples/scenarios/leo_radiometric_weather_frequency.yaml --backend local --output /tmp/astro-dsn-calibration.json` and `astro dsn-calibration examples/scenarios/leo_radiometric_weather_frequency.yaml --measurements /tmp/astro-radiometric-weather-frequency.tdm --format tdm --output /tmp/astro-dsn-calibration-from-tdm.json` after exporting the weather/frequency measurement product to TDM | Writes a suite-owned DSN-style media calibration product from generated two-way/three-way radiometric records or from loaded JSON/CSV/TDM measurement files, including per-record participant path, uplink/downlink media delay, elevation diagnostics when available, source/model provenance, and aggregate delay statistics. TDM export/load preserves suite `ASTRO_*` media-correction metadata for this product surface. This validates the calibration product surface without claiming full ODF/TNF DSN station calibration. |
| Station calibration | `astro station-calibration examples/scenarios/leo_two_station_od.yaml examples/measurements/leo_two_station_od_measurements.json --output /tmp/astro-station-calibration.json` | Writes a per-station/per-measurement-type bias product from measurement records carrying `truth` metadata, including mean/min/max/RMS residuals, population standard deviation, mean absolute residual, sigma min/mean/max, normalized mean/RMS bias, calibrated/uncalibrated counts, and explicit non-native DSN limitations. This is a deterministic suite residual product, not native binary DSN calibration processing or a station coordinate/clock/media solver. |
| Optical measurement synthesis | `astro synth-measurements examples/scenarios/leo_two_station_angles.yaml --backend local --output /tmp/astro-angle-measurements.json`, `astro synth-measurements examples/scenarios/leo_two_station_topocentric.yaml --backend local --output /tmp/astro-topocentric-measurements.json`, `astro synth-measurements examples/scenarios/leo_geodetic_topocentric.yaml --backend local --output /tmp/astro-geodetic-topocentric-measurements.json`, `astro synth-measurements examples/scenarios/leo_geodetic_eop_topocentric.yaml --backend local --output /tmp/astro-geodetic-eop-topocentric-measurements.json`, `astro synth-measurements examples/scenarios/leo_geodetic_eop_table_topocentric.yaml --backend local --output /tmp/astro-geodetic-eop-table-topocentric-measurements.json`, and `astro synth-measurements examples/scenarios/leo_geodetic_precession_nutation_topocentric.yaml --backend local --output /tmp/astro-geodetic-precession-nutation-measurements.json` | Writes inertial RA/Dec, ECI-station local-horizon az/el, WGS-84 geodetic-station az/el, fixed-EOP geodetic-station az/el, interpolated-EOP geodetic-station az/el, and compact precession/nutation geodetic-station az/el records in degrees. |
| Local launch report | `astro report-tuned-launch examples/launch/pitch_program_two_stage.yaml --point-indices 2,3 --iterations 2 --orbit-duration-s 600 --orbit-step-s 60 --output /tmp/astro-launch-report.json` | Writes tuned launch report with insertion and short-arc assessments. |
| RocketPy-configured launch schema | `python -m pytest tests/astro_launch/test_launch_io.py::test_load_rocketpy_configured_launch_scenario tests/astro_launch/test_launch_io.py::test_load_rocketpy_single_stage_launch_scenario -q` | Loads the two-stage RocketPy configuration fixture and the single-stage direct-simulation fixture with typed RocketPy vehicle/motor/flight configuration. |
| Local research propagation | `astro research-propagate examples/scenarios/leo_two_body.yaml --backend local --cases 2 --position-sigma-km 0.01 --velocity-sigma-km-s 0.000001 --seed 7 --output /tmp/astro-research.json` | Writes `MonteCarloResult` with deterministic seeded cases. |

## Optional Backend Gates

These gates run only when the matching runtime is installed. If a runtime is absent, capture the
structured smoke-test or `UnsupportedBackendError` diagnostic and record the gate as not-run rather
than failed or complete.

| Backend | Command | Available Behavior | Unavailable Behavior |
| --- | --- | --- | --- |
| Orekit | `astro orekit-smoke` | Exits 0 with wrapper/JVM/frame/time availability. | Exits 1 with structured JSON explaining missing `orekit-jpype`, Java, or data-context failure. |
| Orekit propagation | `astro propagate examples/scenarios/leo_two_body.yaml --backend orekit --output /tmp/astro-orekit-trajectory.json`, `astro propagate examples/scenarios/leo_j2.yaml --backend orekit --output /tmp/astro-orekit-j2.json`, `astro propagate examples/scenarios/leo_orekit_high_fidelity.yaml --backend orekit --output /tmp/astro-orekit-high-fidelity.json`, `astro propagate examples/scenarios/leo_orekit_drag.yaml --backend orekit --output /tmp/astro-orekit-drag.json`, `astro propagate examples/scenarios/leo_orekit_srp.yaml --backend orekit --output /tmp/astro-orekit-srp.json`, and `astro propagate examples/scenarios/leo_orekit_third_body.yaml --backend orekit --output /tmp/astro-orekit-third-body.json` | Writes suite `Trajectory` with `backend = "orekit"` for two-body, J2, high-fidelity numerical-path, atmospheric-drag, SRP, and Sun/Moon third-body scenarios. | Exits 2 with `UnsupportedBackendError` diagnostics for runtime/data failures. |
| Orekit native OD | `ASTRO_RUN_OREKIT_LIVE=1 python -m pytest tests/astro_backends/test_orekit_estimation.py::test_live_orekit_native_od_executes_batch_estimator -q` and `astro estimate-measurements <geodetic-range-rate-scenario.yaml> <measurements.json> --estimator orekit-native --output /tmp/astro-orekit-native-estimate.json` | Runs Orekit `BatchLSEstimator` for WGS-84 geodetic range/range-rate measurements and returns suite `EstimateResult` with residuals, RMS, iteration/evaluation limits, wrapper provenance, and explicit covariance availability metadata. | Skips the pytest gate unless `ASTRO_RUN_OREKIT_LIVE=1`; CLI exits 2 with `UnsupportedBackendError` diagnostics for runtime/data/measurement-support failures. |
| RocketPy | `astro rocketpy-smoke` | Exits 0 when RocketPy imports and required classes exist. | Exits 1 with structured JSON explaining missing package/import failure. |
| RocketPy launch adapter | `astro launch examples/launch/rocketpy_configured_single_stage.yaml --backend rocketpy --output /tmp/astro-rocketpy-launch.json` and `astro launch examples/launch/rocketpy_configured_two_stage.yaml --backend rocketpy --output /tmp/astro-rocketpy-two-stage-launch.json` | Runs configured solid-rocket flights through RocketPy, returns suite `LaunchTrajectory` products, annotates two-stage suite scenarios with reached stage events/samples around one configured RocketPy flight, and reports stage-schedule completeness plus a multistage adapter contract that identifies the non-native composition boundary. | Exits 2 with structured `UnsupportedBackendError` diagnostics when dependencies/configuration are unavailable. |
| Dymos/OpenMDAO | `astro dymos-smoke` | Exits 0 when Dymos and OpenMDAO APIs import. | Exits 1 with structured JSON explaining missing package/import failure. |
| Dymos launch optimization | `astro optimize-launch examples/launch/pitch_program_two_stage.yaml --backend dymos --output /tmp/astro-dymos-optimized-launch.json` and `astro optimize-launch examples/launch/pitch_program_two_stage.yaml --backend dymos --dymos-mode pitch-program --output /tmp/astro-dymos-pitch-program-launch.json` | Runs the default stage-aware Dymos/OpenMDAO vertical-ascent phase transcription, and can opt into a native Dymos pitch-control transcription over the suite pitch program. Both return suite `LaunchPitchTuningResult` products with Dymos phase diagnostics, stage-plan metadata, original and optimized pitch-program control-point schedules, tuned point indices, path constraints, altitude, velocity, and radial-velocity target-insertion residual/tolerance assessment, explicit pitch-optimization scope metadata, a pitch-program transcription contract with per-stage control coverage, and a stage-schedule coverage flag. | Exits 2 with structured `UnsupportedBackendError` diagnostics when Dymos/OpenMDAO cannot be imported or an unsupported Dymos mode is requested. |
| TudatPy | `astro tudat-smoke` | Exits 0 when TudatPy imports. | Exits 1 with structured JSON; install path is platform-specific and not assumed to be pip-only. |
| JAX/JAXLIB | `astro jax-smoke` | Exits 0 when JAX and `jax.numpy` import; `astro research-propagate --backend jax` can run two-body and J2 seeded ensembles, can map `orekit_high_fidelity` into a J2 screening baseline with configured degree/order provenance when requested, can add research drag/SRP, analytic Sun/Moon third-body, and configured third-body ephemeris sample force flags, can add an opt-in final-state transition matrix with `--include-sensitivities`, and can map `scenario.initial_covariance` to a final covariance through `P_final = Phi * P_initial * Phi^T`; `astro research-od-sensitivity --backend jax` can write range/range-rate, inertial RA/Dec, and local-horizon az/el OD residual Jacobians plus normal/covariance diagnostics, and `astro research-estimate --backend jax` uses backtracking Gauss-Newton corrections for range/range-rate and angular research estimates. | Exits 1 with structured JSON explaining missing `astro-suite[research]` runtime. |

## Reference Tolerances

| Comparison | Current Gate |
| --- | --- |
| Local two-body determinism | Unit tests compare sample count and deterministic products. |
| MEO/GEO examples | Tests validate and propagate the medium-Earth and geosynchronous-radius scenarios. |
| Orekit two-body vs local | `orekit_live` test compares final LEO state with `abs(position) <= 1 km` and `abs(velocity) <= 1e-3 km/s` when `ASTRO_RUN_OREKIT_LIVE=1`. |
| Orekit J2 vs local | `orekit_live` test compares final LEO J2 state with `norm(position delta) < 0.05 km` and `norm(velocity delta) < 1e-4 km/s` when `ASTRO_RUN_OREKIT_LIVE=1`. |
| Orekit covariance propagation | Unit tests verify `propagate_orekit` populates `covariance_history` from `initial_covariance` with Orekit finite-difference state-transition metadata, accumulated STMs, and process-noise matrices. `ASTRO_RUN_OREKIT_LIVE=1 python -m pytest tests/astro_backends/test_orekit_propagation.py::test_live_orekit_covariance_history_returns_suite_product -q` executes the real Java/Orekit path against `examples/scenarios/leo_covariance.yaml`. |
| Orekit native OD bridge | Unit tests map suite WGS-84 geodetic range/range-rate records into one-way Orekit `Range`/`RangeRate` objects, construct a native `BatchLSEstimator` boundary with a numerical propagator builder, execute the estimator through the runtime abstraction, map estimated state, residuals, RMS, covariance, and iteration diagnostics into `EstimateResult`, and verify `astro estimate-measurements --estimator orekit-native` dispatch without requiring Java in the default suite. The opt-in `orekit_live` gate executes the real Java estimator. Singular covariance extraction is reported through `covariance_status = "unavailable"` with a zero fallback and the Orekit error in metadata. |
| RocketPy live launch examples | `ASTRO_RUN_ROCKETPY_LIVE=1 python -m pytest tests/astro_backends/test_rocketpy_simulation.py::test_live_rocketpy_configured_launch_examples_return_suite_products -q` runs the installed RocketPy API against the checked-in single-stage and two-stage configured examples and verifies suite `LaunchTrajectory` products and the two-stage adapter contract without making default CI depend on RocketPy. |
| Dymos live launch optimization | `ASTRO_RUN_DYMOS_LIVE=1 python -m pytest tests/astro_backends/test_dymos_optimization.py::test_live_dymos_optimization_returns_suite_product tests/astro_backends/test_dymos_optimization.py::test_live_dymos_pitch_program_optimization_executes_native_transcription -q` runs the installed Dymos/OpenMDAO APIs against the checked-in pitch-program example and verifies suite `LaunchPitchTuningResult` metadata, including full configured-burn coverage, optimized pitch-program control-point metadata, the default phase contract, and the opt-in native pitch-control transcription contract, without making default CI depend on Dymos. |
| Local finite burns and attitude | Tests compare finite-burn propagation against the no-maneuver local baseline and verify maneuver events, thrust-vector mass depletion, inertial, velocity-aligned, radial-outward, and radial-inward thrust direction modes, commanded-attitude provenance, and diagonal rigid-body torque attitude propagation. |
| Local orbital events | Tests verify local propagation annotates periapsis/apoapsis extrema with epoch, elapsed time, radius, radial-velocity, and root-bracket metadata for no-maneuver trajectories, while preserving sample-safe event annotation for maneuvered trajectories. |
| Conjunction screening | Tests verify time-aligned trajectory screening returns the closest sample, relative state, threshold status, density and integrated covariance-aware encounter-plane probability methods, a public CLI JSON product, and a conjunction assessment report that classifies screening-only, review-required, and operational-candidate readiness without claiming external production validation. |
| Local covariance propagation | Tests verify covariance history length, epoch alignment, symmetry, explicit per-sample and accumulated state-transition matrices, process-noise covariance matrices, finite-difference provenance metadata, opt-in two-body and J2 variational state-transition metadata, and optional white-acceleration process-noise growth. |
| Orekit covariance propagation | Tests verify finite-difference covariance history on Orekit trajectories and require high-fidelity covariance metadata to name the selected transition propagator and force-model list. |
| Launch handoff | Tests confirm `LaunchTrajectory.insertion_state` converts into a normal orbital `Scenario` and propagates locally. |
| OD explicit measurements | Tests validate JSON, CSV, TDM range/range-rate/angle ingest/export, JSON/CSV one-way Doppler preservation, normalized DSN ODF/TNF-style tracking CSV ingest, suite-owned DSN ODF/TNF KVN tracking deck ingest, suite-owned ASTRODSN1 binary tracking bridge ingest, TDM suite-extension Hz Doppler preservation, TDM/JSON/CSV two-way/three-way radiometric preservation with iterative vacuum light-time diagnostics, configured constant and weather/frequency media correction metadata, DSN-style calibration summary and station-bias calibration products, inertial and local-horizon angle generation, WGS-84 geodetic station measurement generation, fixed/tabulated/IERS-finals-ingested EOP geodetic station transforms, angle wrapping, Doppler and radiometric residual prediction, and local least-squares convergence. |
| Research Monte Carlo and OD sensitivities | Tests confirm seeded repeatability, local Monte Carlo provenance, built-in JAX two-body/J2 runner parity against the local reference for zero dispersion, opt-in JAX final-state sensitivity metadata, JAX final-covariance mapping from `scenario.initial_covariance`, JAX range/range-rate, inertial RA/Dec, and local-horizon az/el OD residual Jacobian products with normal/covariance diagnostics, JAX backtracking Gauss-Newton OD estimate products for range/range-rate and topocentric az/el measurements, and explicit JAX research approximations for `orekit_high_fidelity`, configured high-order gravity metadata on a J2 baseline, atmospheric drag, solar radiation pressure, and analytic circular Sun/Moon third-body gravity. Ephemeris-backed third-body force modeling remains gated on validated body construction. |
| Tudat cross-check propagation | Tests verify the default Tudat runner constructs native two-body, J2, atmospheric drag, cannonball SRP, and Sun/Moon point-mass third-body propagation settings through fake TudatPy modules, maps state history into suite trajectories, records provenance, writes finite-difference covariance-history products when `initial_covariance` is supplied, supports the opt-in native variational covariance construction boundary, and rejects unsupported force models. |

## Backend Boundary Rule

No optional backend may return native engine objects through public products. All backends must map
into suite-owned `Trajectory`, `EstimateResult`, `LaunchTrajectory`, `LaunchPitchTuningResult`, or
`MonteCarloResult` models and include backend provenance in product metadata when available.
